/*
	3. Жучки: що неправильно у наступному програмному коді?
*/
class Shape
{
public:
	Shape();
	virtual ~Shape();
	virtual Shape(const Shape&);//так неможна, конструктор копіє не можна создати виртуально, але є метод "clone"  для цього 
};
